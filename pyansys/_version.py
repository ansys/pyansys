"""Version for pyansys landing package

This needs to be incremented to build new notes on the PyPi page.

"""

# major, minor, patch
version_info = 0, 62, 'dev0'

# Nice string for the version
__version__ = '.'.join(map(str, version_info))
